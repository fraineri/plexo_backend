version: "3.9"

services:
  postgres_db:
    image: postgres:16-alpine

    container_name: plexo_db

    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=plexo

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d plexo"]
      interval: 10s
      timeout: 5s
      retries: 5

    restart: unless-stopped

  redis_cache:
    image: redis:7-alpine

    container_name: plexo_redis

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
